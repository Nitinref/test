{
  "name": "@lingoguard/action",
  "version": "1.0.0",
  "description": "GitHub Action for LingoGuard",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist-action",
    "dev": "tsc --watch",
    "clean": "rm -rf dist dist-action"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@octokit/rest": "^20.0.2",
    "@lingoguard/cli": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}